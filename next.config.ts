import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["img.spoonacular.com", "avatars.githubusercontent.com", "github.githubassets.com"],

  }
};

export default nextConfig;
